import { DateFormatePipe } from './date-format.pipe';

describe('DateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
